import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { GeneratequizPage } from './generatequiz';

@NgModule({
  declarations: [
    GeneratequizPage,
  ],
  imports: [
    IonicPageModule.forChild(GeneratequizPage),
  ],
})
export class GeneratequizPageModule {}
