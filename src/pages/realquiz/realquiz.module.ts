import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { RealquizPage } from './realquiz';

@NgModule({
  declarations: [
    RealquizPage,
  ],
  imports: [
    IonicPageModule.forChild(RealquizPage),
  ],
})
export class RealquizPageModule {}
